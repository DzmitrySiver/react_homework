import React from 'react';
import ErrorMessage from '../../../../common/styles/ErrorMessage.styles';

const NoMoviesFoundMessage = () =>
    <ErrorMessage>No movies found</ErrorMessage>;

export default NoMoviesFoundMessage;
